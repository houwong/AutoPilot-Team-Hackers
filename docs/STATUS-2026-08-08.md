# AutoPilot — Status and Plan

**Date:** 8 August 2026
**Branch:** `main` @ `089e581` · working tree clean · 22 PRs merged
**Code freeze:** tonight, 23:59

---

## 1. Where we are

| | |
|---|---|
| Orchestrator | v37, published, **37/37** structural checks |
| Backend tests | **61 passing** |
| Migrations | `g8h9i0j1k2l3` (head) |
| Integrations | **5/5 healthy**, Round 2 floor passing, 3 categories |
| Runtime | 58 runs · 356 operator invocations · 40.7% autonomy · ~140s average |
| Policies | 21 active, editable, applied before the agent acts |
| Workbench | 1 open, 3 resolved |

Run this before any demo — it takes five seconds:

```bash
docker compose exec backend python scripts/check_orchestrator.py
```

It reads the orchestrator definition and fails on faults that are invisible at
runtime. Every check exists because that exact fault shipped at least once.

---

## 2. The agent

All seven operators and the orchestrator were **rebuilt** as a new set and the
Command Center switched to it. All three decision branches are verified live:

```
allow      ITSM-2005  → remediate → notify requester      succeeded
escalate   ITSM-2180  → park for human + Slack alert      awaiting_human
block      ITSM-2065  → notify, no remediation            succeeded
```

Rollback is one line: set `AUTO_WF_ORCHESTRATOR` back to
`019fd290-bdf0-7000-88b9-2b00a7dbb7fc`. Note the old workflows have since been
deleted, so this is now a paper fallback only.

---

## 3. What we fixed, and why it mattered

These were not cosmetic. Each one produced confident, well-formed, wrong output
without raising an error.

### The agent wrote to tickets before the human approved

Operator 3 updated the live ticket **even when it decided a human must review
it**. By the time the reviewer opened the Workbench item, production had already
changed — and rejecting could not undo it. The Supabase write now sits behind
`cond_safe_execute` and only runs on `AUTO_REMEDIATE`.

### The human decision did not complete the workflow

Approving in the Workbench recorded a decision and started a **separate** run,
while the original sat parked at `step_4_rev` forever. That is judging gate #2 —
*"that decision completes the workflow"* — and we were failing it while looking
fine.

Auto **can** be resumed from outside; the endpoint was in the form markup Auto
itself serves. Approving now submits the real form and the paused run continues.

### "Processed" did not mean "the ticket changed"

The queue derived `auto_remediated` from a notification step completing. That is
evidence the orchestrator finished a branch, not that the service desk moved. A
campaign reported ITSM-2003 as `human_approved` while Supabase still held it at
"Waiting for support" with nothing written.

`verification` is now recorded **beside** `outcome`:

- `outcome` — which path did the agent take
- `verification` — did the ticket actually change

Both are shown on Processed Tickets.

### The queue ranked on the wrong field

Candidates were ordered by the stored `Priority` column, so a `Low` ticket
already breaching SLA for a VIP sorted below a `Highest` ticket comfortably
within target. Operator 1 now supplies the ranking, on (SLA status, VIP).

Live: 372 tickets ranked, first breached at position 0, first within-SLA at
position 205.

### Smaller ones

- A parked review no longer blocks the whole queue
- Approve no longer loops forever on a ticket with no KB article
- The AI Manager works — answers from our own records and can trigger runs
- Workbench Refresh re-reads the item you are looking at
- Runs abandoned by a restart are reclaimed instead of showing as in-flight
- The Data Manager reports real per-channel delivery, not just "the step ran"

---

## 4. Known bugs

**None functional.** Remaining items are cosmetic or environmental:

| | item | impact |
|---|---|---|
| 1 | Operator 1's prompt claims a `priority` field its code does not emit | none — everything falls back to Operator 2 |
| 2 | Outlook has a **daily** send quota we have hit once | email fails, Slack unaffected |
| 3 | Operator 3 shows `isDraft: true` at v4 | none — its default version is the latest, verified |

---

## 5. Not tested — please do not assume

1. **Nobody has done a full click-through of the UI in a browser.** We can
   confirm every page returns 200 and the APIs serve correct data, but the pages
   fetch client-side, so what actually paints is unverified. **Biggest gap, and
   the first thing a judge sees.**
2. The agent has run against roughly **12 tickets out of 460**.
3. Operator 5 (SLA) and Operator 6 (incidents) outputs were checked for
   *completion*, not *correctness*.

---

## 6. The plan — what is done and what is left

Against `queue-planning-and-state-consistency-notes.md`:

| § | | status |
|---|---|---|
| 4.1 | Queue Planner ranks the preview | **partial** — see below |
| 4.2 | Keep the execution orchestrator unchanged | done |
| 4.3 | Duplicate protection at three boundaries | done — already existed |
| 4.4 | Supabase reconciliation | done |
| 4.5 | Audit data ownership | done — already matched |

### The one real gap: §4.1 is half implemented

Ranking comes from Operator 1, which is the right owner. But Operator 1 reads
the **stored** `customfield_10030` SLA column, not Operator 5's recomputed
business-hours value, and Operator 6's incident context is not in the ranking at
all.

So success criterion 1 — *"traceable to Operator 5/Operator 1 evidence"* — is
half true: the right ranking rule on second-hand input.

Closing it needs the separate read-only planner workflow built in Supervity
(Operator 5 ‖ Operator 6 → Operator 1 planning mode → end). **Recommendation:
do not attempt this before the freeze.** It touches Auto, and Auto changes have
cost us a working orchestrator more than once.

### One deliberate divergence

The plan's success criterion 8 says the queue should not label a path
`auto_remediated` unless verification succeeds. We record `verification` beside
`outcome` instead of gating the label, because `outcome` is a true fact about
what the agent did and overwriting it loses that. **This is a product call — say
if you would rather it gate.**

### Open decisions (§7) left untouched

They are product calls, not implementation. The most important — *what does
"completed" mean* — no longer needs a single answer: `outcome` and
`verification` are separate fields precisely so "automation done" and "ticket
resolved" do not collapse into one word.

---

## 7. To do before the freeze

| | task | who |
|---|---|---|
| 1 | Full browser click-through of all six pages | Chee Hou |
| 2 | Fresh queue campaign so Processed Tickets is not empty | either |
| 3 | Submission note — guide §10.5 wants outcome metric + integrations | either |
| 4 | Demo rehearsal and agree the narrative | both |

---

## 8. Two rules while testing

**Every agent run writes to live Supabase data.** Several tickets were corrupted
during testing and restored. A guard now refuses runs against closed tickets,
but do not run arbitrary ticket keys.

**Approving or rejecting ITSM-2180 consumes the CAB demo.** It sets `CHG-0001`
away from `Pending CAB Approval`, and the ticket stops escalating — it takes the
block path instead, silently. It has been reset twice today. If you practise
that flow, reset it before the demo:

```sql
-- in Supabase: change_requests
status = 'Pending CAB Approval', approver = null   where change_id = 'CHG-0001'
```

---

## 9. Running it

```bash
docker compose up -d          # everything, prod frontend on :3001
cd frontend && npm run dev    # hot-reload dev frontend on :3000
```

Use **3001** for the demo — that is the production build a judge would see.
Use **3000** while developing; the Docker frontend is a production build and
needs `docker compose up -d --build frontend` for every change.

Both talk to the same backend and database, so data agrees; only code can drift.
